apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "functionator.fullname" . }}-test-connection"
  labels:
    {{- include "functionator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "functionator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
