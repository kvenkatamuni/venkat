apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bolcategory.fullname" . }}-test-connection"
  labels:
    {{- include "bolcategory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bolcategory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
