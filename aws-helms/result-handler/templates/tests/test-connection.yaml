apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "result-handler.fullname" . }}-test-connection"
  labels:
    {{- include "result-handler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "result-handler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
