apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "template-converter.fullname" . }}-test-connection"
  labels:
    {{- include "template-converter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "template-converter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
