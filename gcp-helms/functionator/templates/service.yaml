apiVersion: v1
kind: Service
metadata:
  name: {{ include "functionator.fullname" . }}
  namespace: {{ .Values.nameSpace }}
  labels:
    {{- include "functionator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "functionator.selectorLabels" . | nindent 4 }}
