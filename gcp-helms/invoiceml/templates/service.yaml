apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    jiffy.app: {{ .Release.Name }}
spec:
  type: "NodePort"
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.image.port }}
      name: "invoiceml"
  selector:
    jiffy.app: {{ .Release.Name }}