apiVersion: v1
kind: Service
metadata:
  name: {{ include "bolcategory.fullname" . }}
  namespace: {{ .Values.nameSpace }}
  labels:
    {{- include "bolcategory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bolcategory.selectorLabels" . | nindent 4 }}
